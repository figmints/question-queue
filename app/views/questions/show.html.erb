<body>
    <div class="title">
      Question: <%= @question.title %>
    </div>
    <div class="description">
      Description: <%= @question.description %>
    </div>
    <div class="department">
      Department: <%= @question.department %>
    </div>

    <%= form_for @answers do |f| %>
    <%= f.text_area :answer %>
    <%= f.submit "Answer", class: "button", id: "save_button" %>

    <% end %>


</body>
